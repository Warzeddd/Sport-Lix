// import { z } from "zod";
// import { DateTime } from "@models/types"; // Assuming DateTime is imported from a specific module

// const CycleSchema = z.object({
//   name: z.string(),
//   startDate: DateTime, // Assuming DateTime is properly defined and imported
//   endDate: DateTime, // Assuming DateTime is properly defined and imported
// });

// export default CycleSchema;
