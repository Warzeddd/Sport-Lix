// import { z } from "zod";

// export const MuscleGroupSchema = z.object({
//   id: z.string().uuid().optional(),  // L'ID est optionnel lors de la création
//   name: z.string(),
//   muscles: z.array(z.string()).optional(),  // Liste d'IDs de muscles
//   movements: z.array(MovementSchema).optional(),  // Liste de mouvements associée
// });

// export type MuscleGroupType = z.infer<typeof MuscleGroupSchema>;
